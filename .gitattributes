# ★줄바꿈을 건드리지 않는다★
# 이 저장소는 파일마다 줄바꿈이 섞여 있다(대부분 LF, data/master.json은 CRLF).
# core.autocrlf 설정이 켜진 PC에서 받으면 git이 자동 변환을 걸어,
# 한 글자도 안 고친 파일이 '전 줄 변경'으로 잡힌다(2026-08-18 실제로 겪음).
# -text = 변환하지 않고 바이트 그대로 둔다. 어느 PC에서 받아도 결과가 같다.
* -text
